import World.Plant;

public class Grass extends Plant {
	public Grass() {
		// System.out.println(this.height);
		// Doesn't work because there is no access specifier on height so it is only accessible in the world package
	}
}
